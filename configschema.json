{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
	"additionalProperties": false,

	"properties": {
		"actApiKey": {
			"type": "string",
			"description": "Key for accessing ACT Api Endpoints",
			"default": "CHANGEME"
		}
  },
  "required": ["actApiKey"]
}
